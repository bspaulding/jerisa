<%= form_for(@food_order) do |f| %>
  <% if @food_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_order.errors.count, "error") %> prohibited this food_order from being saved:</h2>

      <ul>
      <% @food_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
